<div class="container pl-5 pr-5 pb-5">
  <div class="row ml-3">
    <h2 class="mt-3 mb-3">ユーザー詳細</h2>
  </div>

  <div class="row mb-1 ml-3 mr-3 pl-4 pr-4">
    <table class="table table-bordered">
      <tr>
        <th class="table-dark">ニックネーム</th>
        <td><%= @customer.nickname %></td>
      </tr>
      <tr>
        <th class="table-dark">自己紹介</th>
        <td><%= @customer.introduction %></td>
      </tr>
      <tr>
        <th class="table-dark">フォロー</th>
        <td>
          <%= link_to followings_customer_path(@customer) do %>
            <%= @followings.count %>
          <% end %>
        </td>
      </tr>
      <tr>
        <th class="table-dark">フォロワー</th>
        <td>
          <%= link_to followers_customer_path(@customer) do %>
            <%= @followers.count %>
          <% end %>
        </td>
      </tr>
    </table>
  </div>

  <div class="row ml-5">
    <% if @customer != current_customer %>
      <% if current_customer.following?(@customer) %>
        <%= link_to 'フォロー外す', customer_relationships_path(@customer), method: :delete, class: "btn btn-outline-danger" %>
      <% else %>
        <%= link_to 'フォローする', customer_relationships_path(@customer), method: :POST, class:"btn btn-outline-success" %>
      <% end %>
    <% end %>
  </div>
  <div class="m-5">
    <%= render 'customer/games/index', games: @games %>
  </div>
</div>
