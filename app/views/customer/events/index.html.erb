<h3>イベントカレンダー</h3>

<div class= 'calender'>
  <%= month_calendar events: @events do |date, events| %>
    <%= date.day %>
    <% events.each do |event| %>
    <div>
      <%= link_to event.title, event_path(event) %>
    </div>
    <% end %>
  <% end %>
</div>